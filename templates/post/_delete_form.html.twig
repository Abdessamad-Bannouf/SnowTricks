<form method="post" action="{{ path('post_delete', {'slug': post.slug}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette figure ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.slug) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
