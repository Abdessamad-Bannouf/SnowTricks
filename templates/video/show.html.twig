{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ video.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('video_index') }}">Revenir à la liste de videos</a>

    <a href="{{ path('video_edit', {'id': video.id}) }}">Editer une video</a>

    {{ include('video/_delete_form.html.twig') }}
{% endblock %}
